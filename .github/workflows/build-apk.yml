name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
      - name: Build debug APKs
        run: |
          echo "sdk.dir=$ANDROID_HOME" > android/local.properties
          cd android
          chmod +x gradlew
          ./gradlew assembleViewerDebug assembleChatDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: gsgs-apks
          path: |
            android/app/build/outputs/apk/viewer/debug/*.apk
            android/app/build/outputs/apk/chat/debug/*.apk
